# URL del backend WebSocket que corre en la PC del usuario.
# Por defecto el frontend se conecta al backend local del propio usuario.
# Déjalo así para la Opción A (cada usuario corre backend/main.py en su PC).
VITE_WS_URL=ws://localhost:8081
